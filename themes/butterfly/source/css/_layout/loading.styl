if hexo-config('preloader.enable')
  if hexo-config('preloader.load_style') == 'wooy'
    @import './_load/wooy'
  else if hexo-config('preloader.load_style') == 'pokemon'
    @import './_load/pokemon'